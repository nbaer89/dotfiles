# This file is committed to the repo with the default (non-NixOS) value.
# On NixOS, chezmoi detects this via run_once script that sets isNixOS=true
# in ~/.config/chezmoi/chezmoi.toml (local override, not committed).
isNixOS = false
